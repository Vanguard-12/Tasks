# LangGraph Reflective Agent

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
python cli.py "Explain the difference between tool and resource in MCP to a student."
```

The script will output the final answer after up to two revisions.
